// import 'package:animalrace/app/data/providers/printer_provider.dart';
// import 'package:get/get.dart';

class PrinterRepository {
  // final _administratorProvider = Get.find<PrinterProvider>();

  // Future<(String, String)> printer(String username, String password) async => await (username, password);//async => await _administratorProvider.printer(username, password);
}
