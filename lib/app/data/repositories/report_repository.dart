// import 'package:animalrace/app/data/providers/report_provider.dart';
// import 'package:get/get.dart';

class ReportRepository {
  // final _administratorProvider = Get.find<ReportProvider>();

  // Future<(String, String)> report(String username, String password) async => await (username, password);//async => await _administratorProvider.report(username, password);
}
